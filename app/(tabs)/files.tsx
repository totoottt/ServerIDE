import { useState } from "react";
import { Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { IconSymbol } from "@/components/ui/icon-symbol";
import { useColors } from "@/hooks/use-colors";
const entries = [{ name: "nginx", type: "folder", size: "—", color: "#4DA3FF" }, { name: "server.js", type: "file", size: "18 KB", color: "#B58CFF" }, { name: ".env.production", type: "file", size: "2 KB", color: "#FFB547" }, { name: "deploy.sh", type: "file", size: "6 KB", color: "#35D07F" }, { name: "docker-compose.yml", type: "file", size: "4 KB", color: "#46D8E7" }];
export default function FilesScreen() { const colors = useColors("dark"); const [path, setPath] = useState("/home/deploy"); const [selected, setSelected] = useState<string | null>(null); const [notice, setNotice] = useState("5 items"); return <ScreenContainer className="px-5" containerClassName="bg-background"><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><View style={styles.header}><View><Text style={[styles.kicker, { color: colors.muted }]}>REMOTE STORAGE</Text><Text style={[styles.title, { color: colors.foreground }]}>Files</Text></View><Pressable onPress={() => setNotice("New folder action ready")} style={({ pressed }) => [styles.add, { backgroundColor: "#4DA3FF", opacity: pressed ? 0.75 : 1 }]}><Text style={{ fontSize: 22, color: "#07111B", lineHeight: 22 }}>+</Text></Pressable></View><View style={[styles.pathBar, { backgroundColor: colors.surface, borderColor: colors.border }]}><IconSymbol name="paperplane.fill" size={16} color="#4DA3FF" /><Text style={[styles.path, { color: colors.foreground }]} numberOfLines={1}>{path}</Text><Pressable onPress={() => setNotice("Path copied")}><Text style={{ color: colors.primary, fontSize: 11, fontWeight: "800" }}>COPY</Text></Pressable></View><View style={styles.toolbar}><Text style={[styles.sub, { color: colors.muted }]}>{notice}</Text><View style={styles.toolButtons}><Pressable onPress={() => setNotice("Sort: name") }><Text style={{ color: colors.primary, fontSize: 11, fontWeight: "800" }}>SORT</Text></Pressable><Pressable onPress={() => setNotice("Refresh complete")}><Text style={{ color: colors.primary, fontSize: 11, fontWeight: "800" }}>REFRESH</Text></Pressable></View></View><View style={[styles.list, { backgroundColor: colors.surface, borderColor: colors.border }]}>{entries.map((entry, index) => <Pressable key={entry.name} onPress={() => { setSelected(entry.name); if (entry.type === "folder") setPath(`${path}/${entry.name}`); }} style={({ pressed }) => [styles.entry, index < entries.length - 1 && { borderBottomWidth: 1, borderBottomColor: colors.border }, pressed && { opacity: 0.65 }]}><View style={[styles.fileIcon, { backgroundColor: `${entry.color}18` }]}><IconSymbol name={entry.type === "folder" ? "paperplane.fill" : "chevron.left.forwardslash.chevron.right"} size={18} color={entry.color} /></View><View style={styles.flex}><Text style={[styles.fileName, { color: colors.foreground }]}>{entry.name}</Text><Text style={[styles.sub, { color: colors.muted }]}>{entry.type === "folder" ? "Folder" : "Text file"}</Text></View><Text style={[styles.size, { color: colors.muted }]}>{entry.size}</Text><Pressable onPress={() => setNotice(`${entry.name} actions opened`)} style={styles.more}><Text style={{ color: colors.muted, fontSize: 18 }}>•••</Text></Pressable></Pressable>)}</View>{selected && <View style={[styles.preview, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={styles.previewHeader}><View><Text style={[styles.kicker, { color: colors.muted }]}>SELECTED ITEM</Text><Text style={[styles.previewTitle, { color: colors.foreground }]}>{selected}</Text></View><Pressable onPress={() => setSelected(null)}><Text style={{ color: colors.primary, fontWeight: "800", fontSize: 12 }}>CLOSE</Text></Pressable></View><Text style={[styles.code, { color: colors.foreground }]}># Server IDE preview\n\nconnection: ready\nstatus: healthy\nlast_sync: just now</Text><View style={styles.previewActions}><Pressable onPress={() => setNotice("Download started")} style={[styles.smallAction, { backgroundColor: "#4DA3FF18" }]}><Text style={{ color: colors.primary, fontWeight: "800", fontSize: 11 }}>DOWNLOAD</Text></Pressable><Pressable onPress={() => setNotice("Delete needs confirmation")} style={[styles.smallAction, { backgroundColor: "#FF5C7018" }]}><Text style={{ color: "#FF5C70", fontWeight: "800", fontSize: 11 }}>DELETE</Text></Pressable></View></View>}</ScrollView></ScreenContainer>; }
const styles = StyleSheet.create({ content: { paddingTop: 12, paddingBottom: 32 }, header: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", marginBottom: 20 }, kicker: { fontSize: 11, fontWeight: "800", letterSpacing: 1.3 }, title: { fontSize: 30, fontWeight: "800", marginTop: 6 }, add: { width: 40, height: 40, borderRadius: 13, alignItems: "center", justifyContent: "center" }, pathBar: { minHeight: 46, borderWidth: 1, borderRadius: 15, flexDirection: "row", alignItems: "center", gap: 10, paddingHorizontal: 13 }, path: { flex: 1, fontSize: 12, fontFamily: "monospace" }, toolbar: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", paddingVertical: 14 }, toolButtons: { flexDirection: "row", gap: 18 }, sub: { fontSize: 10 }, list: { borderRadius: 18, borderWidth: 1, paddingHorizontal: 14 }, entry: { minHeight: 68, flexDirection: "row", alignItems: "center", gap: 11 }, fileIcon: { width: 34, height: 34, borderRadius: 11, alignItems: "center", justifyContent: "center" }, flex: { flex: 1 }, fileName: { fontSize: 13, fontWeight: "700" }, size: { fontSize: 10 }, more: { width: 25, alignItems: "center" }, preview: { borderRadius: 18, borderWidth: 1, marginTop: 15, padding: 15 }, previewHeader: { flexDirection: "row", justifyContent: "space-between", alignItems: "flex-start" }, previewTitle: { fontSize: 17, fontWeight: "800", marginTop: 4 }, code: { backgroundColor: "#0B0F13", borderRadius: 12, padding: 14, fontFamily: "monospace", fontSize: 11, lineHeight: 18, marginTop: 14 }, previewActions: { flexDirection: "row", gap: 9, marginTop: 12 }, smallAction: { flex: 1, alignItems: "center", paddingVertical: 11, borderRadius: 11 } });
