# ServerIDE 0.7.0 — معالجة ملاحظات ملف الأخطاء

هذه حزمة مصدر وليست IPA مبنيًا. تبقى شهادة BOOS بنوع ad_hoc ومعرّف app.carambola5307.spinach7929 دون تغيير. رقم البناء 9.

## ما تغيّر

| الملاحظة | التنفيذ في المصدر |
| --- | --- |
| لا يظهر التطبيق في «الملفات» | Info.plist صريح بمفتاحَي المشاركة الصحيحين، إنشاء Welcome.txt دون استبدال الموجود، شاشة حالة تفحص إعدادات التطبيق المثبّت، وفحص الحزمة النهائية في Codemagic |
| فشل حفظ المحرر | إرسال أجزاء صغيرة؛ استبدال ذري بعد فحص النسخة الأصلية؛ نسخة احتياطية مخفية بجانب الملف؛ الحفاظ على المالك والأذونات والخصائص الممتدة؛ منع الكتابة إذا تغيّر الملف |
| خطأ IP العام | إظهار رمز HTTP وسبب الرفض؛ فصل جلب العنوان العام عبر ipify عن معلومات الموقع عبر ipapi.co؛ إبقاء العنوان عند تعطل خدمة الموقع |
| صعوبة العودة إلى SSH | تبويب Terminal دائم يعرض الجلسات القائمة؛ فتح السيرفر يعيد استخدام تبويبه؛ زر + وحده ينشئ جلسة إضافية |
| عدد النوافذ لا يعود للصفر | العدّاد يعكس عدد الجلسات الفعلي؛ Close tab يغلق التبويب بدل الاكتفاء بإخفاء الشاشة؛ لا تُغلق جلسة أثناء تنفيذ أمر |
| المجلدات مختلطة بالملفات | قسم مستقل للمجلدات وآخر للملفات، خط أسماء أوضح، وإبقاء إعداد إظهار الملفات المخفية |
| التفاف النص | إعادة حساب عرض النص عند تغيّر حجم الشاشة، مع التفاف على مستوى الأحرف للمخرجات الطويلة |
| لا توجد أيقونة | أيقونة أصلية 1024×1024 غير شفافة داخل AppIcon؛ يتحقق البناء من بيانات الأيقونة النهائية |
| طرفية محلية | داخل Tools → Local Terminal: pwd، ls -a، cd، cat، mkdir، touch، cp، mv، rm، echo، clear؛ محصورة في Documents، والحذف ينقل إلى .Trash |
| حساب GitHub | داخل Tools → GitHub: تحقق فعلي بالرمز، تخزين في Keychain، عرض المستودعات بصفحات، وروابطها. لا توجد عملية استنساخ أو رفع كود |
| رسوم مراقبة السيرفر | CPU من فروق عدادات /proc/stat، الذاكرة، ومعدل نقل الشبكة من عينات فعلية كل 10 ثوانٍ أثناء عرض الشاشة في المقدمة |

## أين يظهر مجلد التطبيق؟

المسار المتوقع بعد بناء هذه النسخة وفتحها: الملفات → استعراض → على الـ iPhone → ServerIDE → Downloads.

المفاتيح في المصدر لا تثبت وحدها الظهور على الجهاز. لذلك يتحقق scripts/verify-bundle.py من Info.plist داخل التطبيق/IPA الناتج فعليًا، وتعرض Settings → Apple Files status & downloads حالة النسخة المثبتة. لم نفحص الجهاز نفسه من هذه البيئة.

## ما تم التحقق منه هنا

- 30 اختبار Python ناجحًا على Linux: العمليات البعيدة، حفظ النصوص الكبير والمجزأ، النسخ الاحتياطية، التعارضات، الروابط، الأذونات والخصائص الممتدة، والتحقق من الحزم باستخدام نموذج اختبار.
- تحليل البنية النحوية لـ50 ملف Swift دون أخطاء؛ ليس ترجمة أو فحص أنواع باستخدام SDK.
- فحص YAML وJSON وplist وصياغة أوامر البناء، ومطابقة معرّف الحزمة ونوع التوقيع.
- فحص الأيقونة بصريًا ومقاسها 1024×1024 وغياب الشفافية.
- 23 حالة XCTest موجودة في المشروع، ولم تُشغّل هنا. يشغّلها Codemagic بمحاكي iPhone؛ لا تُتجاوز عند الفشل.

## حدود مهمة

- مصدر عدم ظهور «الملفات» على الجهاز غير مؤكد دون فحص IPA السابق؛ التعديل يزيل الاعتماد على توليد مفاتيح plist ويضيف فحصًا للحزمة.
- خطأ NIO المنشور لا يثبت أن حجم الأمر هو السبب الوحيد. أُزيل مسار الحفظ بأمر واحد ضخم، لكن الاتصال الحي يحتاج اختبار Xcode/الجهاز.
- الطرفية المحلية ليست bash ولا Python ولا ios_system. ترفض الأوامر غير المدعومة والروابط الرمزية، ولا تصل إلى ملفات تطبيقات أخرى.
- الطرفية البعيدة ما زالت أوامر منفصلة وليست PTY تفاعلية دائمة؛ لا يدعم هذا الإصدار nano/vim والمطالبات التفاعلية.
- Apple Files يعرض التنزيلات المحلية، لا نظام ملفات السيرفر المباشر. ما زال File Provider بعيد يحتاج تنفيذًا مستقلًا.
- لا توجد إشعارات APNs خارجية، ولا خادم push. لم تُستخدم خدمة أو رموز الإشعارات الظاهرة في صورك.
- رسوم الشبكة إجمالية وقد تحسب واجهات Docker الافتراضية حركةً أكثر من مرة. لا تتضمن جميع تفاصيل TCP/UDP/ICMP أو كل واجهة/قرص الواردة في الصور المرجعية.
- العمليات السابقة للخدمات وDocker لا تعادل كامل واجهة Compose الموضحة في الصور.
- لا يوجد استنساخ Git محلي أو push؛ المصادقة وقائمة المستودعات فقط.
- فحص تعارض حفظ الملف ليس قفلًا عامًا يمنع برامج أخرى من الكتابة؛ توجد نافذة سباق صغيرة بين الفحص والاستبدال الذري.
- ملفات النسخ الاحتياطية تبقى على السيرفر ولا تُحذف تلقائيًا. الحفظ يرفض الروابط الرمزية والصلبة.
- التحقق من بصمة SSH ما زال يقبل جميع المفاتيح في الكود الموروث؛ الإصدار ليس جاهزًا للنشر العام قبل معالجة ذلك.

## البناء

ضع محتويات مجلد ServerIDE في جذر المستودع نفسه ثم شغّل ServerIDE Ad Hoc IPA. لا تحذف التطبيق المثبّت ولا تغيّر Bundle ID. بعد نجاح الاختبارات والتوقيع يتحقق البناء من مفاتيح Files والأيقونة داخل IPA قبل تسليمه.

مراجع: [Apple File Provider](https://developer.apple.com/documentation/fileprovider)، [ipify](https://www.ipify.org/)، [GitHub REST](https://docs.github.com/en/rest/users/users#get-the-authenticated-user).
