# ملاحظات ملف جميع الطلبات — 2026-08-30

## الصفحات 1-4

تؤكد الصفحات الأولى أن هناك خطأ وظيفيًا ما زال يمنع الدخول العملي إلى **SSH** أو تصفح **الملفات** بالشكل المتوقع، وأن المطلوب ليس فقط تشغيل الخدمة بل جعلها مستقرة وقابلة للاستخدام اليومي.

كما يوضح المستخدم بعبارة صريحة أن **الصور من تطبيقات أخرى هي للشرح فقط**، وأنه لا يريد نسخًا مباشرًا للتصميم، بل يريد **شيئًا خاصًا وأصليًا** لهوية Server IDE. هذا يفرض قيدًا إبداعيًا واضحًا: نأخذ من المرجع منطق التدفق والوظائف واتساع الأقسام، لا التخطيط البصري ولا شكل القوائم ولا الهوية الرسومية.

تستخرج الصفحات أيضًا مطالب وظيفية مهمة. أولها أن التطبيق يجب أن يحتوي **عددًا مناسبًا من التبويبات والأقسام** يغطي احتياجات المستخدم الصغير والمتقدم معًا. وثانيها أن داخل SSH يجب توفير **تحديد النص بالكامل** وعمليات **النسخ واللصق** بشكل مريح. وثالثها أن المطلوب هو وجود **أقسام متعددة للأدوات** تتجاوز مجرد SSH وSFTP، مثل أدوات العناوين والشهادات وDNS وفحص الشبكات وأدوات النصوص والـHTTP والمتصفح والأدوات المساندة.

من الصفحة الرابعة يظهر أن المستخدم مهتم أيضًا ببنية معلوماتية غنية داخل التطبيق، تشمل أقسامًا مثل **Servers، Forwardings، Snippets، Tools، Known Hosts، History، Settings، Terminal & SSH، SSH Keys** وغيرها. لكن النص المرافق يوضح مرة أخرى أن هذه أمثلة على الاتساع الوظيفي وليست واجهات يجب استنساخها.

الخلاصة المرحلية أن هذه الصفحات لا تغيّر فقط بعض الشاشات، بل تضيف مبدأ تصميم أساسي: **نأخذ نطاق الوظائف وسير الاستخدام من المرجع، ونبني واجهة أصلية خاصة بـServer IDE تتفوّق بالوضوح والهوية، لا بالتقليد**.

## الصفحات 5-8

تضيف هذه الصفحات تفاصيل عملية جدًا تخص تجربة **الطرفية** وتجربة **الأدوات**. أولًا، يريد المستخدم أن تكون **أيقونات النسخ واللصق واضحة داخل SSH**، وألا يقتصر السلوك على لصق فقط، بل يشمل تحديدًا ونسخًا فعليًا للنص الكامل أو الجزئي. ثانيًا، يشير المرجع إلى أهمية وجود **شريط أدوات خاص بالطرفية** مناسب للهاتف، لكن المستخدم يطلب أن يكون هذا الشريط **بتصميم خاص بنا** لا مطابقًا للصورة.

كما تُظهر الصفحات الحاجة إلى **قائمة إجراءات سياقية** داخل مساحة العمل، تشمل مثلًا Notes وUpload وCopy وPaste وTools وExport، ما يعني أن واجهة الملفات والطرفية ينبغي أن تدعم إجراءات سريعة مرتبطة بالسياق بدل أزرار جامدة أو مشتتة.

وتبرز كذلك مطالبة صريحة بأن يمتلك التطبيق **مخزن أوامر محفوظة Snippets** أو أوامر متكررة يحتاجها المستخدم، وأن يكون ذلك جزءًا واضحًا من تجربة SSH بدل كونه تفصيلًا ثانويًا.

الصفحة الثامنة تؤكد أن عرض **الرامات، والاستهلاك، وحالة السيرفر، والمؤشرات التفاعلية** يجب أن يكون غنيًا وواضحًا، وأن الألوان تتغير بحسب الحالة. كما تضيف مطلبين مهمين في تجربة الاستخدام: قدرة المستخدم على **إخفاء الكيبورد داخل SSH** عند الحاجة، ودعم **تغيير الخط** مع أكثر من خط إنجليزي وعربي، إضافة إلى **تكبير حجم الكتابة** بما يناسب القراءة الطويلة على الهاتف.

إذن، من هذه الصفحات تتحدد أربعة متطلبات تنفيذية جديدة: شريط أدوات طرفية أصلي وواضح، دعم نسخ/لصق/تحديد فعلي داخل SSH، مخزن أوامر متكررة، وتحكم أفضل بالخط والكيبورد والعرض التفاعلي للمؤشرات.

## الصفحات 9-12

توضح هذه الصفحات أن شاشة **Servers** نفسها تحتاج إلى سلوك أغنى عند الضغط المطول على اسم أو بطاقة الخادم، بحيث تظهر **قائمة سياقية** تتضمن إجراءات مثل SSH وSFTP وToday وRecent وEdit وClone وNotes وRecordings وFavorite وTools وCopy وDelete. المطلوب هنا ليس نسخ القائمة كما هي بصريًا، بل بناء تجربة أصلية داخل Server IDE تجعل بطاقة الخادم نقطة انطلاق مركزية لكل ما يتعلق بذلك الخادم.

كما تشير الملاحظات النصية إلى أن داخل قسم **Tools** يجب توفير أدوات عملية مثل **Ping** و**Traceroute** و**Port Scan**، وأن خيار **Copy** المقترح في المرجع مرتبط بنسخ بيانات اتصال مفيدة مثل Host وUser وPassword. وبما أن كلمة المرور عنصر حساس، فالتنفيذ المناسب في Server IDE يجب أن يكون منضبطًا أمنيًا، مثل النسخ الصريح بعد تأكيد المستخدم أو إخفاء/إظهار القيم بدقة، لا عرضها افتراضيًا.

وتؤكد الصفحتان 10 و11 أن شاشة **الملفات** تحتاج إلى خيارات أكثر نضجًا، مثل **Create directory** و**Create file** و**Upload file** و**Upload folder** و**Favorites** و**Hide hidden files** و**Jump to**، مع هوية مرئية أكثر عصرية وأناقة من المرجع، ومع دعم أوضح لتصغير الخط وتكبيره داخل المستعرض.

تضيف الصفحة 11 مطلبًا مهمًا لتجربة الإنتاجية: المستخدم يريد القدرة على **فتح أكثر من نافذة أو جلسة داخل SSH**، كما يريد أسلوب عرض ملفات/أماكن/مواقع مفضل أو مهيكل يساعده على التنقل السريع. وتصر الصفحة نفسها على وجود خيار **إظهار الملفات المخفية** داخل المتصفح.

أما الصفحة 12 فتفتح محورًا مستقلًا يتعلق بالتكاملات، إذ تطلب **مصادقة ودخولًا على حساب GitHub**. هذا ينسجم مع فكرة المنتج العام متعدد المستخدمين، لكنه لا يغيّر القيد الأساسي: التكامل يُبنى بلغتنا التصميمية الخاصة، لا بأسلوب التطبيق المرجعي.

النتيجة من هذه الدفعة أن التطبيق يحتاج إلى ثلاث طبقات إضافية فوق الموجود حاليًا: **قوائم سياقية غنية لكل خادم ولكل ملف، أدوات شبكية مرتبطة بالخادم المختار، وتكامل حساب اختياري مثل GitHub**، مع بقاء الشكل النهائي مبتكرًا وأصيلًا.

## الصفحات 13-16

تنتقل هذه الصفحات من المتطلبات العامة إلى مستوى أدق في **لوحة المراقبة Metrics**. يظهر منها أن المستخدم يريد إضافة خدمات وأقسام جديدة أسفل التنقل الحالي، مع تركيز خاص على **الإحصاءات الحية** الخاصة بالخادم، مثل CPU وMemory وDisk وNetwork Interface وNetwork Protocol، إضافة إلى بطاقات أو أقسام قابلة للطي والتوسيع تعرض بيانات متعددة الطبقات.

كما يتضح أن المطلوب ليس مجرد أرقام، بل **عرض تفاعلي بصري** يتضمن رسومًا صغيرة، حالات لونية، ومؤشرات صعود وهبوط للرفع والتنزيل، مع بنية تسمح بقراءة سريعة على شاشة الهاتف. المرجع يوضح طبيعة المعلومات المطلوبة، لكن ليس الشكل النهائي الواجب نسخه؛ لذلك ينبغي تحويل هذا المفهوم إلى لغة تصميم Server IDE الخاصة، مع المحافظة على طابع احترافي أكثر ووضوح أعلى في التسلسل البصري.

ومن خلال تسلسل الصور، يمكن استنتاج أن تجربة المراقبة التي يريدها المستخدم تشمل ما يلي: **ملخص نظام التشغيل، تبويبات زمنية أو فلاتر زمنية، تقسيم الموارد إلى وحدات مستقلة، وقابلية الانتقال بين بطاقات المراقبة والشبكات دون ازدحام**. وهذا يتفق مع الطلب السابق الخاص بتلوين الحالات دلاليًا وبأن يكون كل شيء تفاعليًا وليس مجرد ديكور.

بالتالي، هذه الصفحات تعزز أولوية ربط شاشات **Processes وNetwork وChecks** ببيانات حقيقية، ثم إعادة إخراجها ضمن بنية مبتكرة من وحدات مراقبة قابلة للتخصيص والتوسعة، بدل الاستمرار في بطاقات تجريبية ثابتة.

## الصفحات 17-20

تكشف هذه الصفحات متطلبات إضافية تخص **المشاريع، الحاويات، الإشعارات، والأدوات السريعة**. تظهر شاشة Project Detail مع إجراءات مثل Logs وStart وStop وRestart وDown ورفع ملفات الإعداد، ما يعني أن المستخدم يريد أن يمتد Server IDE إلى إدارة خدمات أو حاويات أو مشاريع تشغيلية، وليس فقط جلسة SSH منفصلة.

كما تظهر صفحة Notifications التي تشير إلى رغبة في وجود **تنبيهات تشغيلية** مرتبطة بحالة الخادم، مثل أخطاء CPU أو الاستخدام المرتفع أو الأعطال. المرجع يوضح الفكرة العامة، لكن واجهة Server IDE يجب أن تقدم ذلك بأسلوب أصلي وأكثر وضوحًا، مع اعتماد ألوان الحالات والتنبيه النصي المباشر.

وفي الصفحتين 19 و20 يظهر تجميع أكثر نضجًا لقائمة **Tools** داخل سياق المراقبة، مثل Process List وSFTP وTCP Ping وTraffic Stats وIP Address، مع استمرار عرض بروتوكولات الشبكة ومؤشراتها. هذا يعزز توجهًا مهمًا في المنتج: الأدوات ليست تبويبًا منفصلًا فقط، بل **مجموعة وحدات وظيفية قابلة للاستدعاء من داخل الخادم النشط**.

بناءً على ذلك، تتأكد ثلاثة محاور تصميمية وتنفيذية: الحاجة إلى طبقة إدارة خدمات/حاويات/مشاريع، الحاجة إلى مركز تنبيهات عملي، والحاجة إلى هيكلة أدوات الخادم داخل وحدات يمكن استدعاؤها من أكثر من شاشة، مع بقاء الهوية البصرية أصلية وغير منسوخة.

## الصفحات 21-24

تواصل هذه الصفحات التركيز على **المراقبة التفصيلية**، وتؤكد أن تجربة المقاييس يجب أن تكون متعددة المستويات: مؤشرات عامة في الأعلى، ثم وحدات تفصيلية قابلة للطي مثل Memory وDisk وNetwork Interface وCPU مع رسوم صغيرة وقراءات لحظية. المطلوب في Server IDE ليس نسخ شكل البطاقات، بل بناء تجربة مراقبة أصلية تحافظ على السرعة والوضوح في القراءة بيد واحدة.

وتظهر الصفحتان 23 و24 بشكل مباشر متطلبات مهمة لشاشة **الملفات**. هناك طلب صريح بأن يكون **عرض الملفات داخله جميع الأوامر** الأساسية، وتشمل: **Create a directory، Search، Create a file، Upload a file، Upload a folder، Add this folder to favorites، Hide hidden files، Jump to**. هذا يثبت أن مستعرض الملفات يجب أن يتحول إلى مساحة عمل متقدمة، لا مجرد قائمة استعراض وفتح.

كما يمكن استنتاج أن المستخدم يريد الانتقال السريع بين المواقع المهمة داخل الشجرة، مع إمكان **إخفاء الملفات المخفية** و**إضافة المجلدات المفضلة**، وهي عناصر إنتاجية مهمة جدًا في منتج إدارة خوادم فعلي. وبما أن المستخدم شدد على الأصالة، فإن الأنسب هو تنفيذ هذه الأوامر داخل شريط إجراءات أو لوحة أوامر خاصة بـServer IDE بدل تقليد قوائم التطبيق المرجعي.

الخلاصة لهذه الدفعة أن أولوية الملفات أصبحت أعلى: يجب تحويل شاشة Files إلى **مستعرض أوامر وسياق** متكامل، مع بحث، ومجلدات مفضلة، وإظهار/إخفاء الملفات المخفية، وانتقال سريع، وإنشاء ملف/مجلد، ورفع ملف/مجلد، مع واجهة أصلية مبتكرة.

## الصفحات 25-27

تحسم الصفحات الأخيرة متطلبات الأوامر السياقية في شاشة الملفات. في مستوى المجلد الحالي يريد المستخدم: **Create a directory، Create a file، Upload a file، Upload a folder، Add this folder to favorites، Hide hidden files، Jump to**. وعند الضغط على مجلد أو ملف، يجب أن تظهر إجراءات مرتبطة به تشمل على الأقل **Copy، Download، Rename، Delete**، مع خيارات نسخ أدق مثل **Path** و**Name**.

هذه الصفحات لا تضيف نموذج واجهة جديدًا يجب تقليده؛ لكنها تؤكد أن مستعرض الملفات في Server IDE يجب أن يمتلك طبقتين واضحتين من الإجراءات: لوحة أوامر للمجلد الحالي، ولوحة إجراءات للعنصر المحدد. ينبغي أن تظهر تلك الإجراءات ضمن تصميم Server IDE الخاص، مع إبقاء الأفعال الخطرة مثل Delete خلف تأكيد واضح، وإظهار حالة النقل في واجهة التقدم القابلة للاستئناف التي أضيفت سابقًا.

## خلاصة الملف كاملًا

الملف يطلب منتج إدارة خوادم أوسع من الواجهة الحالية: جلسات SSH متعددة وحية، أدوات طرفية قابلة للنسخ واللصق والاختصارات، Snippets، إدارة خوادم وقوائم سياقية، مستعرض ملفات غني، أدوات شبكة ومقاييس حية، مشاريع أو حاويات، تنبيهات، وتكامل GitHub اختياري. لكن القيد الحاكم الذي كرره المستخدم هو أن المرجع يشرح **ماذا يجب أن يفعل المنتج** لا **كيف يجب أن يبدو**. وعليه سيُبنى الاتجاه البصري التالي من مبادئ Server IDE: طبقات مراقبة تشبه لوحة تشغيل، مسارات تنفيذ واضحة، ألوان دلالية مضبوطة، وإجراءات سياقية هادئة وسريعة، بلا نسخ للبطاقات أو شريط التنقل أو التخطيطات الموجودة في التطبيقات المصوّرة.
