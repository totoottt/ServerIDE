# Verification notes — 2026-08-30

تم تشغيل المعاينة بعد تحويل SSH وFiles إلى tRPC/SFTP. شاشة SSH تظهر حالة IDLE عند عدم اختيار خادم، وتعرض زر إدخال بيانات الاعتماد وسجل الطرفية بدون IP أو بيانات ثابتة. شاشة Files تظهر المسار الجذري، زر AUTH، زر REFRESH، ولا تعرض ملفات تجريبية عند غياب بيانات الاتصال. هذا يتوافق مع شرط عدم إظهار بيانات خادم شخصية أو أرقام mock.

الفحوص الأخيرة: TypeScript وVitest وESLint نجحت بعد إضافة ssh2. لا يوجد اختبار اتصال فعلي بخادم خارجي في بيئة التطوير الحالية، لذلك تبقى مصادقة الخادم وبصمة المضيف بحاجة إلى اختبار يدوي بخادم يملكه المستخدم.
