# خطة تصميم Server IDE Mobile

## الرؤية

يقدم التطبيق تجربة جوال داكنة ومركزة لإدارة الخوادم والخدمات من شاشة واحدة، مع واجهات تشعر المستخدم بحالة النظام لحظة بلحظة. لن يكون الهدف نسخ الصور المرجعية، بل تحويل مبادئها إلى هوية خاصة: بطاقات عملية، مؤشرات حية، حالات لونية مفهومة، وأزرار تقدم استجابة واضحة دون ازدحام بصري.

يفترض التصميم استخدام الهاتف بالوضع العمودي وبيد واحدة. لذلك ستوضع الإجراءات المهمة في النصف السفلي أو في شريط الإجراءات السفلي، وستكون مناطق اللمس لا تقل عن حجم مناسب للمس، مع تجنب الأزرار الصغيرة المتجاورة.

## قائمة الشاشات

| الشاشة | المحتوى والوظيفة الأساسية |
| --- | --- |
| لوحة التحكم | ملخص صحة الخادم، الاتصال الحالي، CPU والذاكرة والتخزين، آخر النشاط، وإجراءات سريعة للخدمات الأكثر استخدامًا. |
| العمليات | قائمة العمليات الجارية مع PID والأمر والمستخدم والحالة واستهلاك CPU والذاكرة، مع البحث والتصفية وفتح تفاصيل العملية. |
| تفاصيل العملية | بطاقة معلومات كاملة للعملية، شريط حالة واضح، زر إيقاف خطر مع تأكيد، وسجل زمني مختصر. |
| الشبكة | اختيار واجهة الشبكة، إجمالي الرفع والتنزيل، نطاق زمني، مخطط حركة، وتنبيه الوضع التجريبي عند عدم توفر البيانات الحية. |
| TCP Ping | اختيار الوجهة، بدء/إيقاف الفحص، الكمون الحالي، Connected/Failed، إحصاءات Min/Max/Avg/Packet Loss، ومخطط تاريخي. |
| SSH / الطرفية | قائمة جلسات الاتصال، خطوات الاتصال المتتابعة، سجل الاتصال القابل للطي، محرر/طرفية، وأدوات سريعة مثل Ctrl وTab وEsc. |
| الملفات | مستعرض ملفات هرمي، مسار حالي، بحث، تحديد متعدد، إنشاء مجلد/ملف، نسخ ونقل وحذف مع تأكيد، ومعاينة الملف النصي. |
| فحوصات النطاق | عرض حالة SPF وDKIM وDMARC وBlacklists وSMTP، مع حالة إجمالية وشارات Info/Warning/Good وفتح تفاصيل كل فحص. |
| المزيد والإعدادات | تفضيلات الثيم، معلومات الاتصال، إدارة الخدمات، ومسح البيانات المحلية عند الحاجة. |

## نظام الألوان الدلالي

| الدلالة | اللون المقترح | الاستخدام |
| --- | --- | --- |
| الخلفية الأساسية | `#07090C` | خلفية التطبيق والمساحات الواسعة. |
| السطح | `#12161B` | البطاقات والعناصر المرتفعة. |
| السطح الثاني | `#1A2027` | الصفوف الفرعية وحقول الإدخال. |
| النص الأساسي | `#F4F7FA` | العناوين والقيم المهمة. |
| النص الثانوي | `#8E9AA8` | الوصف والطوابع الزمنية. |
| اللون الأساسي | `#4DA3FF` | التنقل النشط، المعلومات، والإجراءات غير الخطرة. |
| نجاح | `#35D07F` | Good، Connected، Ready، نجاح العملية. |
| تحذير | `#FFB547` | Warning، Demo Mode، حالة تحتاج انتباهًا. |
| خطأ/خطر | `#FF5C70` | Failed، Kill، الحذف، وفشل الاتصال. |
| بنفسجي قياسي | `#B58CFF` | إحصاءات إضافية وحالات التشخيص. |
| سماوي قياسي | `#46D8E7` | الشبكة والاتصال والرسوم المساندة. |

يجب ألا يعتمد المستخدم على اللون وحده؛ كل حالة ستعرض أيضًا نصًا وشارة أو أيقونة. وستستخدم الألوان القوية باعتدال داخل شارات أو حدود أو نقاط مؤشر حتى تبقى القراءة مريحة في الوضع الداكن.

## بنية التنقل

سيستخدم التطبيق شريط تبويب سفلي ثابت بخمس وجهات عملية: **Dashboard، Processes، Network، SSH، Files**. ستظهر وجهة More ضمن زر إضافي أو شاشة إعدادات داخلية. عند فتح تفاصيل عملية أو جلسة أو ملف، يستخدم التطبيق شاشة داخلية مع زر رجوع واضح في أعلى اليسار.

## المكونات التفاعلية

ستعتمد كل بطاقة قابلة للفتح على حالة ضغط مرئية، وتنتقل إلى شاشة التفاصيل عند اللمس. أزرار الإجراءات الأساسية ستعرض حالة الضغط والتحميل والنجاح أو الخطأ، وستستخدم اهتزازًا خفيفًا في الإجراءات الأساسية عند توفره. إجراءات الخطر ستفتح تأكيدًا واضحًا يشرح الأثر قبل التنفيذ، بينما تعرض العمليات الناجحة رسالة قصيرة وشارة خضراء قابلة للملاحظة.

ستستخدم الرسوم المختصرة في الشبكة وTCP Ping لشرح الاتجاه وليس للزينة فقط. وستحتوي كل شاشة على حالة تحميل وحالة فارغة وحالة خطأ؛ أما البيانات غير المتاحة فستظهر كـ`غير متاح` أو رسالة دلالية بدل أرقام وهمية.

## تدفقات الاستخدام الرئيسية

### مراقبة الخادم

يفتح المستخدم لوحة التحكم، يرى حالة الاتصال والبطاقات الملخصة، ثم يضغط بطاقة CPU أو الذاكرة لفتح تفاصيل الخدمة ذات الصلة. يمكنه العودة بالسحب أو زر الرجوع دون فقد حالة التصفية.

### إيقاف عملية

يفتح المستخدم شاشة العمليات، يختار عملية، يراجع الأمر والمستخدم والاستهلاك، ثم يضغط زر الإيقاف الأحمر. يظهر تأكيد يذكر PID والأمر، وبعد التأكيد تتحول الحالة إلى `Stopping` ثم `Stopped` أو `Failed` مع نتيجة واضحة.

### فحص الاتصال

يفتح المستخدم TCP Ping، يختار وجهة أو يكتبها، ثم يبدأ الفحص. تظهر الحالة `Connecting` باللون الأصفر، ثم `Connected` بالأخضر أو `Failed` بالأحمر، مع تحديث الإحصاءات والمخطط أثناء التشغيل. زر الإيقاف يبقى ظاهرًا في موضع سهل الوصول.

### فتح جلسة SSH

يختار المستخدم جلسة محفوظة، يرى مراحل Network ثم Auth ثم Shell ثم PTY ثم Ready. كل مرحلة مكتملة تحمل علامة نجاح خضراء، والمرحلة الحالية تحمل مؤشرًا أزرق، والفشل يظهر برسالة قابلة لإعادة المحاولة. بعد الجاهزية تظهر الطرفية وأدوات لوحة المفاتيح السريعة.

### إدارة الملفات

يفتح المستخدم Files، يتنقل بين المجلدات، يضغط ملفًا لمعاينته أو يضغط زر الإجراء لنسخه أو نقله أو حذفه. الحذف دائمًا يتطلب تأكيدًا، والنجاح يظهر بالأخضر والخطأ بالأحمر مع تفسير قابل للفهم.

## قواعد الإخراج البصري

ستكون العناوين قوية وقصيرة، والقيم الرقمية كبيرة بما يكفي للقراءة السريعة. ستتجنب الشاشات الجداول العريضة، وستستبدلها بصفوف بطاقات متجاوبة. ستبقى أهم الإجراءات ضمن الثلث السفلي من الشاشة، بينما تعرض التفاصيل الثانوية داخل أقسام قابلة للطي لتقليل التمرير.

## نطاق التنفيذ الأولي

سيتم تنفيذ تجربة محلية تفاعلية ببيانات حالة منظمة وقابلة للتبديل، مع تجهيز البنية لاستبدالها بمصادر حقيقية لاحقًا. لن تتم إضافة تسجيل دخول أو مزامنة سحابية أو تخزين خلفي ما لم يطلب المستخدم ذلك صراحة؛ وسيركز الإصدار الأول على جودة الواجهة والتفاعل والتنقل وعرض الحالات.


## النظام الأساسي لـMobile Server IDE

### نموذج المنتج

يقدم التطبيق بيئة عمل للمطور على iPhone وiPad تجمع Dashboard وServer Manager وWorkspace وSSH Terminal وSFTP وCode Editor وWeb Preview وأدوات الشبكة والمراقبة والمشاريع في تجربة واحدة مترابطة. في مرحلة التصميم الحالية تعمل الخدمات ببيانات تجريبية عامة قابلة للتبديل، ولا يُستخدم أي IP أو مستخدم أو خادم شخصي ثابت.

يبدأ المستخدم بوضع ضيف دون تسجيل دخول، أو يختار تسجيل الدخول اختياريًا إذا أراد مزامنة إعداداته بين أجهزته. يضيف خادمًا من خلال Host أو IP والمنفذ واسم المستخدم وطريقة المصادقة، ثم يختار اسمه ومجموعته ولونه وأيقونته وملاحظاته. لا تُطلب بيانات اعتماد حقيقية في مرحلة التصميم، ولا تُحفظ أسرار فعلية داخل الواجهة التجريبية.

### خريطة الشاشات

| القسم | الشاشات والوظائف |
| --- | --- |
| Overview | Dashboard، الخوادم النشطة، الاتصالات الأخيرة، الملفات والمشاريع الأخيرة، الصحة والتنبيهات والإجراءات السريعة |
| Servers | قائمة الخوادم، إضافة وتعديل وحذف، مجموعات Production وDevelopment وPersonal وTesting، البحث والمفضلة |
| Workspace | Files وEditor وTerminal وPreview وLogs، مع انتقال سريع وSplit View على iPad |
| Terminal | جلسات SSH تجريبية، تبويبات، History، أوامر محفوظة، اختصارات Ctrl وAlt وEsc وTab، حجم الخط والثيم |
| Files | تصفح المجلدات، معاينة، إنشاء، إعادة تسمية، نقل، نسخ، حذف، رفع وتنزيل كواجهات تجريبية |
| Code | محرر نصي تجريبي، تبويبات، أرقام أسطر، بحث واستبدال، حجم الخط، اللغات والثيمات |
| Tools | IP وDNS وPing وPort Test وWHOIS وSSL وHTTP Inspector وAPI Tester وTCP Ping |
| Monitor | CPU وRAM وDisk وLoad وUptime والعمليات وServices وDocker وSystem Logs |
| Projects | المشروع، الخادم، المجلد، Git، أمر التشغيل، Preview URL وسجل المخرجات |
| Settings | الثيم، اللغة، الخط، التبويبات، الاختصارات، ترتيب الأدوات، المزامنة الاختيارية وقفل التطبيق |

### تخصيص المستخدم

يشمل نموذج التخصيص هوية الخادم، ترتيب الأقسام، الأدوات الظاهرة، العناصر المفضلة، أوامر snippets، إعدادات الطرفية، ألوان الحالات، حجم الخط، الثيم، تخطيط Workspace، وتفضيلات العرض. تُحفظ هذه التفضيلات محليًا في وضع الضيف، وتُجهز نقطة مزامنة اختيارية عند تسجيل الدخول لاحقًا.

### دلالات الألوان

الأخضر يدل على اتصال أو عملية ناجحة، والأحمر يدل على فشل أو إجراء خطر، والبرتقالي يدل على تحذير أو اقتراب انتهاء، والأزرق يدل على معلومات أو إجراء قابل للتنفيذ، والبنفسجي يدل على تخصيص أو أدوات متقدمة، والرمادي يدل على حالة غير نشطة أو غير متاحة. يجب أن يظهر اللون مع نص أو رمز واضح، وليس كإشارة لونية وحيدة.

### حدود المرحلة الحالية

المرحلة الحالية مخصصة للتصميم والتفاعل والبيانات التجريبية باستخدام Expo/React Native. الاتصال الحقيقي بـSSH وSFTP وتشغيل الأوامر والمزامنة والخدمات المدفوعة ستُبنى لاحقًا فوق هذه النماذج دون تغيير هوية التطبيق أو مسار Expo الحالي.


## جلسة SSH حية ونقل ملفات قابل للاستئناف

### مدخل الخادم وبيانات الاعتماد

وفق المرجع المرفق، يجب أن تفتح بطاقة Active Server تدفق الاتصال مباشرة، وأن تتضمن شاشة تحرير الخادم ضمن قسم SSH/SFTP حقول User وPrivate Key وJump Host وForce Keyboard Interactive وPassword مع إظهار/إخفاء كلمة المرور وPort. تُعرض بيانات الاعتماد بطريقة آمنة، ولا تُكتب في سجل الطرفية أو في الحزمة. ستبقى كلمة المرور والمفتاح في ذاكرة الجلسة، مع خيار حفظ آمن لاحقًا عبر Keychain عند اعتماد المنتج النهائي.

### جلسة PTY في الوقت الفعلي

سيستخدم SSH قناة PTY حية عبر WebSocket. عند فتح الجلسة تنشأ جلسة ذات معرف ثابت وتُرسل أحداث الاتصال والمخرجات والأخطاء فورًا. يعرض التطبيق Network وAuth وShell وPTY وReady، مع حالة Connecting وLive وReconnecting وClosed. كل إدخال من لوحة المفاتيح أو الاختصارات مثل Ctrl وEsc وTab يمر إلى القناة الحية، ولا يعود التطبيق إلى تنفيذ أوامر منفصلة عبر HTTP إلا كخطة احتياطية.

### نقل الملفات الكبير

كل Upload أو Download ينشئ Transfer Job بمعرف ثابت، والمصدر والوجهة والحجم والجزء الحالي والبايتات المؤكدة وspeed وETA. ينقل الملف على أجزاء، وبعد تأكيد كل جزء يسجل الخادم checkpoint ويكتب التطبيق نسخة محلية من وصف المهمة. تعرض الشاشة progress bar ونسبة مئوية وحجمًا منقولًا من الإجمالي والسرعة والوقت المتبقي وحالة الشبكة وزري Pause وResume.

### الاستئناف بعد الانقطاع

إذا انقطع الاتصال أو أُغلق التطبيق، تحفظ آخر نقطة مؤكدة فقط. عند العودة يعيد التطبيق تحميل Transfer Job من التخزين المحلي، ويتحقق الخادم من حجم الملف الجزئي وchecksum للأجزاء المؤكدة، ثم يستأنف من offset التالي باستخدام append أو ranged transfer. لا تُعاد الأجزاء المؤكدة ولا يُعلن اكتمال الملف قبل مطابقة الحجم النهائي وchecksum. حالات النقل الدلالية هي Paused بالبرتقالي، Reconnecting بالأزرق، Resuming بالبنفسجي، Completed بالأخضر، وFailed بالأحمر، مع نص واضح لا يعتمد على اللون وحده.

### بنية التنفيذ

تظل tRPC مناسبة لإدارة إنشاء المهمة والاستعلام عن حالتها، بينما تستخدم جلسة PTY قناة WebSocket مستقرة. تحتاج عمليات النقل الكبيرة إلى endpoint للأجزاء أو قناة ثنائية الاتجاه تبث أحداث progress، مع سجل دائم للـcheckpoint حتى يعمل الاستئناف عند تعدد نسخ الخادم. سيُختبر السلوك بخادم SSH تجريبي مملوك للمستخدم لأن انقطاع الشبكة الحقيقي والصلاحيات لا يمكن إثباتهما من المعاينة وحدها.
